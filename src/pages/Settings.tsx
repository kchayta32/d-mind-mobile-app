
import React from 'react';
import UserPreferencesSettings from '@/components/preferences/UserPreferencesSettings';

const Settings = () => {
  return <UserPreferencesSettings />;
};

export default Settings;
